import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "InvoiceFlow — Invoice & Customer Management",
  description:
    "A modern invoice and customer management platform. Effortlessly create, track, and manage invoices and customers.",
  openGraph: {
    title: "InvoiceFlow",
    description: "Modern invoice and customer management",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
